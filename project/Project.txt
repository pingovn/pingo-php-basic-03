Project 1: 
	- Create website allow user register, logged-in, logged-out, 
					update account information
	- Create admin webpage to manage users, 
					create front-end page for user login
	- (*) Create simple news page to allow users post news with images, 
					categories, search. Admin/editor can edit/approve. 
					(**) Users can comment, like, etc...
Project 2:
	- Create website allow user register, logged-in, logged-out, 
					update account information
	- Create admin webpage to manage users, 
					create front-end page for user login
	- (*) Create simple products page to allow users post products with images, 
					categories, search. 
					(**) Users can comment, like, etc...

Project 3:
	- Create website allow user register, logged-in, logged-out, 
					update account information
	- Create admin webpage to manage users, 
					create front-end page for user login
	- (*) Create simple video page to allow users post video with video link, 
					categories, search. 
					(**) Users can comment, like, etc...
					
Project 4:
	- Create website allow user register, logged-in, logged-out, 
					update account information
	- Create admin webpage to manage users, 
					create front-end page for user login
	- (*) Create simple Ad page (like chotot.vn) to allow users post Ad with 
					Ad infomation such as areas (District in HCM City), categories,
					ad title, ad description, ad image, time created.
					Search function. 
					(**) Users can comment, like, etc...
					